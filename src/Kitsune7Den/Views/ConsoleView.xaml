<UserControl x:Class="Kitsune7Den.Views.ConsoleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Style="{StaticResource SectionHeader}" Text="Console" DockPanel.Dock="Left" />
            <Button Content="Clear" Style="{StaticResource SecondaryButton}" DockPanel.Dock="Right"
                    Command="{Binding ClearCommand}" HorizontalAlignment="Right" />
        </DockPanel>

        <!-- Log Output -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="0">
            <ListBox x:Name="LogListBox" ItemsSource="{Binding LogLines}"
                     Background="Transparent" Foreground="{DynamicResource TextPrimaryBrush}"
                     BorderThickness="0" FontFamily="Consolas" FontSize="12"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.VirtualizationMode="Recycling">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="8,2" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                        <Setter Property="BorderThickness" Value="0" />
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Border>

        <!-- Command Input -->
        <DockPanel Grid.Row="2" Margin="0,12,0,0">
            <Button DockPanel.Dock="Right" Content="Send" Style="{StaticResource ActionButton}"
                    Command="{Binding SendCommandCommand}" Margin="8,0,0,0" />
            <TextBox Style="{StaticResource DarkTextBox}" Text="{Binding CommandInput, UpdateSourceTrigger=PropertyChanged}"
                     FontFamily="Consolas" FontSize="13"
                     x:Name="CommandBox" KeyDown="CommandBox_KeyDown" />
        </DockPanel>
    </Grid>
</UserControl>
