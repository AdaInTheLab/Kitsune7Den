<UserControl x:Class="Kitsune7Den.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="OnLoaded">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Style="{StaticResource SectionHeader}" Text="Server Logs" DockPanel.Dock="Left"
                       VerticalAlignment="Center" Margin="0" />
            <Button Content="Refresh" Style="{StaticResource SecondaryButton}" DockPanel.Dock="Right"
                    Command="{Binding RefreshCommand}" HorizontalAlignment="Right" />
        </DockPanel>

        <!-- Log selector + filter -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="300" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Log file dropdown -->
            <ComboBox Grid.Column="0" ItemsSource="{Binding LogFiles}"
                      SelectedItem="{Binding SelectedLog}" />

            <!-- Filter -->
            <TextBox Grid.Column="2" Style="{StaticResource DarkTextBox}"
                     Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged, Delay=300}"
                     Tag="Filter logs (e.g. ERR, INF, player name...)" />

            <Button Grid.Column="3" Style="{StaticResource SecondaryButton}" Padding="8,6" Margin="6,0,0,0"
                    Command="{Binding OpenInExplorerCommand}" ToolTip="Open in Explorer">
                <TextBlock Text="&#xE838;" FontFamily="Segoe MDL2 Assets" FontSize="13" />
            </Button>
        </Grid>

        <!-- Log content -->
        <Border Grid.Row="2" Background="#FF0d0d1a" CornerRadius="6"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <ListBox ItemsSource="{Binding Lines}"
                     Background="Transparent" Foreground="{DynamicResource TextPrimaryBrush}"
                     BorderThickness="0" FontFamily="Consolas" FontSize="11"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.VirtualizationMode="Recycling">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="8,1" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                        <Setter Property="BorderThickness" Value="0" />
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Border>

        <!-- Status -->
        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Foreground="{DynamicResource TextSecondaryBrush}"
                   FontSize="12" Margin="0,8,0,0" />
    </Grid>
</UserControl>
